#!/usr/bin/env node

'use strict'

const {execSync} = require('child_process')
const command = `ozy rollup ${process.argv[2]} | browserify -`

if (process.env.NODE_ENV === 'production') {
  command += ' | uglifyjs -m -c warnings=false'
}

execSync(command, {stdio: 'inherit'})
